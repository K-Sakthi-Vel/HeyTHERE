{
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "scripts":{
    "build":"npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix Client_side && npm run build --prefix Client_side"
  }
}
